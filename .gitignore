# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Index data — regenerable, never committed. Anchored to the repo root so the
# src/tracepaper/index package is not caught by these.
*.db
*.db-wal
*.db-shm
/data/
/index/

# Local config / secrets
.env
config.local.*

# OS
.DS_Store

# Build
build/
dist/
.pytest_cache/
